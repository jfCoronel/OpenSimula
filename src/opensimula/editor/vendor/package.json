{
  "name": "opensimula-editor-vendor",
  "private": true,
  "description": "Builds the anywidget editor's static/editor.js: editor.src.js bundled with its dependencies (Ajv) into one self-contained ESM module with no runtime imports of any kind, since anywidget loads it from a blob URL where relative/bare imports can't resolve, and VS Code's notebook webview CSP additionally blocks fetching third-party scripts.",
  "scripts": {
    "build": "esbuild editor.src.js --bundle --format=esm --outfile=../static/editor.js"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  },
  "dependencies": {
    "ajv": "8.17.1"
  }
}
